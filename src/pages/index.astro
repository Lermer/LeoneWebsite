---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Leone Ermer - Home">
  <svg style="position:absolute;width:0;height:0;">
    <filter id="noise">
      <feTurbulence type="fractalNoise" baseFrequency="0.8" numOctaves="4" stitchTiles="stitch"/>
    </filter>
  </svg>
  <div class="hero">
    <h1>
      <span class="trigger" data-image="https://picsum.photos/400/500?random=1">Leone Ermer</span><wbr> is a creative working across<wbr> <span class="trigger" data-image="https://picsum.photos/400/500?random=3">experiential design</span>,<wbr> <span class="trigger" data-image="https://picsum.photos/400/500?random=2">branding</span>,<wbr> and&nbsp;<span class="trigger" data-image="https://picsum.photos/400/500?random=4">art</span>,<wbr> based in Los&nbsp;Angeles.
    </h1>
    <div class="hover-image" id="hoverImage">
      <img src="" alt="">
    </div>
  </div>
</BaseLayout>

<style>
  :global(body) {
    overflow: hidden;
  }

  :global(footer) {
    display: none;
  }

  .hero::before {
    content: '';
    position: absolute;
    inset: 0;
    filter: url(#noise);
    opacity: 0.035;
    mix-blend-mode: overlay;
    pointer-events: none;
    z-index: 1;
  }

  .hero {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    background: linear-gradient(
      0deg,
      #FFFFFF 0%,
      #F0F8FF 4%,
      #D4EEFF 7.5%,
      #B0E0FF 10%,
      #7ED4FF 14.9%,
      #5EC5FF 22%,
      #3FB5FF 30%,
      #2A9BFF 38%,
      #1A82FF 46%,
      #0D6AFF 54%,
      #0058FF 62%,
      #0048FF 69.33%,
      #0040EE 80%,
      #003ADE 100%
    );
    background-size: 100% 150vh;
    background-position: 0% 0%;
    overflow: hidden;
    display: grid;
    grid-template-columns: repeat(8, 1fr);
    column-gap: 20px;
    padding: 0 40px;
    animation: sunrise 4s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }

  @keyframes sunrise {
    from {
      background-position: 0% 0%;
    }
    to {
      background-position: 0% 100%;
    }
  }

  h1 {
    position: fixed;
    left: 40px;
    bottom: 20px;
    grid-column: 1 / 6;
    width: calc((100vw - 80px - 140px) * 5 / 8 + 80px);
    font-family: 'new-spirit-condensed', serif;
    font-style: normal;
    font-weight: 700;
    font-size: 78px;
    line-height: 120%;
    color: #000000;
    margin: 0;
    transform: rotate(-0.48deg);
    opacity: 0;
  }

  h1.ready {
    opacity: 1;
  }

  .trigger {
    position: relative;
    cursor: pointer;
    transition: opacity 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    will-change: opacity;
  }

  @media screen and (min-width: 900px) {
    .trigger:hover {
      opacity: 0.3;
    }
  }

  .hover-image {
    position: fixed;
    top: 0;
    left: 0;
    pointer-events: none;
    opacity: 0;
    transform-origin: center center;
    transition: opacity 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    z-index: 9999;
    will-change: transform, opacity;
    --extraOffset: -1.125;
  }

  .hover-image.active {
    opacity: 1;
  }

  .hover-image img {
    width: 400px;
    height: 500px;
    object-fit: cover;
    border-radius: 6px;
    box-shadow:
      0 2px 8px rgba(0, 0, 0, 0.06),
      0 8px 24px rgba(0, 0, 0, 0.12),
      0 16px 48px rgba(0, 0, 0, 0.16);
    transform-origin: center center;
    opacity: 0;
    transform: scale(0.5);
    transition:
      transform 0.4s cubic-bezier(0.16, 1, 0.3, 1),
      opacity 0.4s cubic-bezier(0.16, 1, 0.3, 1),
      box-shadow 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .hover-image.active img {
    opacity: 1;
    transform: scale(1);
  }

  @media (max-width: 900px) {
    .hero {
      padding: 0 30px;
      column-gap: 15px;
    }

    h1 {
      font-size: 59px;
      left: 30px;
      width: calc((100vw - 60px - 105px) * 5 / 8 + 60px);
      grid-column: 1 / 6;
    }

    .hover-image img {
      width: 300px;
      height: 375px;
    }
  }

  @media (max-width: 640px) {
    .hero {
      padding: 30px 20px 80px 20px;
      column-gap: 10px;
    }

    h1 {
      font-size: 44px;
      left: 20px;
      bottom: 20px;
      width: calc((100vw - 40px - 70px) * 7 / 8 + 60px);
      grid-column: 1 / 8;
    }

    .hover-image img {
      width: 250px;
      height: 312px;
    }
  }
</style>

<script>
  const triggers = document.querySelectorAll('.trigger');
  const hoverImage = document.getElementById('hoverImage') as HTMLElement;
  const img = hoverImage?.querySelector('img') as HTMLImageElement;
  const h1 = document.querySelector('h1') as HTMLElement;

  // Wait for fonts to load + ensure the browser has painted opacity:0 first
  document.fonts.ready.then(() => {
    requestAnimationFrame(() => {
      requestAnimationFrame(() => {
        h1?.classList.add('ready');
      });
    });
  });

  // Simple delay/lag with linear interpolation (no bounce)
  let targetX = 0;
  let targetY = 0;
  let currentX = 0;
  let currentY = 0;
  let targetRotation = 0;
  let currentRotation = 0;

  const lerp = 0.06; // Interpolation factor (lower = more delay)
  const rotationLerp = 0.08; // Slightly faster for rotation

  // Track mouse position and calculate subtle rotation
  let lastMouseX = 0;
  document.addEventListener('mousemove', (e) => {
    const deltaX = e.clientX - lastMouseX;
    targetX = e.clientX;
    targetY = e.clientY;
    // Subtle rotation based on horizontal movement (-3 to 3 degrees)
    targetRotation = Math.max(-3, Math.min(3, deltaX * 0.15));
    lastMouseX = e.clientX;
  });

  // Simple lerp animation loop (no bounce, just smooth delay)
  function animate() {
    if (hoverImage?.classList.contains('active')) {
      // Smoothly interpolate toward target (no bounce)
      currentX += (targetX - currentX) * lerp;
      currentY += (targetY - currentY) * lerp;
      currentRotation += (targetRotation - currentRotation) * rotationLerp;

      // Get image dimensions
      const rect = img.getBoundingClientRect();

      // Position from bottom-left corner using transform (hardware accelerated)
      const x = currentX;
      const y = currentY - rect.height;
      hoverImage.style.transform = `translate3d(${x}px, ${y}px, 0) rotate(${currentRotation}deg)`;
    } else {
      // Reset rotation when not active
      currentRotation += (0 - currentRotation) * rotationLerp;
    }
    requestAnimationFrame(animate);
  }
  animate();

  triggers.forEach(trigger => {
    trigger.addEventListener('mouseenter', (e) => {
      const target = e.target as HTMLElement;
      const imageSrc = target.dataset.image;

      if (imageSrc && img) {
        img.src = imageSrc;

        // Initialize position to avoid jump
        const mouseEvent = e as MouseEvent;
        const rect = img.getBoundingClientRect();
        currentX = mouseEvent.clientX;
        currentY = mouseEvent.clientY;

        const x = currentX;
        const y = currentY - rect.height;
        hoverImage.style.transform = `translate3d(${x}px, ${y}px, 0)`;

        hoverImage?.classList.add('active');
      }
    });

    trigger.addEventListener('mouseleave', () => {
      hoverImage?.classList.remove('active');
    });
  });
</script>
